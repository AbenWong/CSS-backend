CREATE TABLE `css_dev_db`.`acc_users` (
             `uid` VARCHAR(45) NOT NULL,
             `create_ time` DATETIME NOT NULL,
             `acc_pw` VARCHAR(45) NOT NULL,
             PRIMARY KEY (`uid`));